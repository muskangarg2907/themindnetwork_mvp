version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: themind
      POSTGRES_PASSWORD: themindpass
      POSTGRES_DB: themind
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  db-data:
